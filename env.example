# Feishu/Lark Application Configuration
# 飞书/Lark 应用配置
APP_ID=your_app_id_here
APP_SECRET=your_app_secret_here
# For Lark users, use https://open.larksuite.com
# Lark 用户请使用 https://open.larksuite.com
LARK_DOMAIN=https://open.feishu.cn

# AI Model Configuration
# AI 模型配置
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
# 服务器配置
PORT=3000

# Redis Configuration (Optional - defaults to internal Redis service)
# Redis 配置（可选 - 默认使用内置 Redis 服务）
# If using external Redis, uncomment and configure:
# 如果使用外部 Redis，请取消注释并配置：
# REDIS_URL=redis://your_redis_host:6379
# REDIS_URL=redis://:password@your_redis_host:6379/0

# Image Generation API Configuration (Optional)
# 图片生成 API 配置（可选）
IMAGE_GENERATION_API_URL=
IMAGE_GENERATION_TOKEN=
IMAGE_GENERATION_DEFAULT_MODEL=gemini-3-pro-image-preview
IMAGE_GENERATION_REFERER=
